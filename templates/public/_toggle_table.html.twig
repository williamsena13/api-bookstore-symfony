{#
  Componente: pub-table-card com toggle
  Parâmetros:
    - icon: string (pi-*)
    - icon_color: string (hex)
    - title: string
    - count: int
    - count_label: string
    - table_id: string (único por página)
    - headers: array de strings
    - rows: array de arrays
#}
