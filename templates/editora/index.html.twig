{% extends 'admin/layout.html.twig' %}
{% block title %}Editoras — {{ livraria.nome ?? 'Bookstore' }}{% endblock %}
{% block page_title %}Editoras{% endblock %}
{% block breadcrumb %}<i class="pi pi-angle-right"></i> Editoras{% endblock %}

{% block content %}
<div class="p-card">
    <div class="p-card-header">
        <h2 class="p-card-title"><i class="pi pi-building"></i> Editoras</h2>
        <div class="d-flex align-items-center gap-2">
            <span style="font-size:.82rem;color:var(--text-secondary);">{{ paginacao.total }} editoras</span>
            <button class="p-btn p-btn-success p-btn-sm btn-modal" data-url="{{ path('app_editora_new') }}" data-title="Nova Editora">
                <i class="pi pi-plus"></i> Nova Editora
            </button>
        </div>
    </div>

    <div class="p-card-body" style="padding:0;">
        <div class="dt-wrapper">
            <table class="table table-striped table-bordered table-hover mb-0">
                <thead>
                    <tr>
                        <th style="width:60px;">#</th>
                        <th>Nome</th>
                        <th style="width:110px;">Livros</th>
                        <th style="width:140px;">Criado em</th>
                        <th style="width:90px;">Ações</th>
                    </tr>
                </thead>
                <tbody>
                    {% for editora in paginacao.items %}
                    <tr>
                        <td class="text-muted">{{ editora.id }}</td>
                        <td><strong>{{ editora.nome }}</strong></td>
                        <td>
                            {% if editora.total_livros > 0 %}
                                <span class="p-badge p-badge-primary"><i class="pi pi-book"></i> {{ editora.total_livros }}</span>
                            {% else %}
                                <span class="p-badge p-badge-secondary">0</span>
                            {% endif %}
                        </td>
                        <td class="text-muted" style="font-size:.8rem;">{{ editora.created_at|date('d/m/Y H:i') }}</td>
                        <td>
                            <div class="flex gap-1">
                                <a href="{{ path('app_editora_edit', {id: editora.id}) }}" class="p-btn p-btn-warning p-btn-sm" title="Editar">
                                    <i class="pi pi-pencil"></i>
                                </a>
                                <form method="post" action="{{ path('app_editora_delete', {id: editora.id}) }}" class="d-inline">
                                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ editora.id) }}">
                                    <button type="button" class="p-btn p-btn-danger p-btn-sm btn-delete" data-label='"{{ editora.nome }}"' title="Excluir">
                                        <i class="pi pi-trash"></i>
                                    </button>
                                </form>
                            </div>
                        </td>
                    </tr>
                    {% else %}
                    <tr><td colspan="5" style="text-align:center;padding:2rem;color:var(--text-secondary);">Nenhuma editora cadastrada.</td></tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>

    {% include 'admin/_pagination.html.twig' %}
</div>

{% include 'admin/_modals.html.twig' %}
{% endblock %}

{% block javascripts %}
<script src="/js/modal-crud.js"></script>
{% endblock %}
