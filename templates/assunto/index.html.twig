{% extends 'admin/layout.html.twig' %}

{% block title %}Assuntos — {{ livraria.nome ?? 'Bookstore' }}{% endblock %}
{% block page_title %}Assuntos{% endblock %}
{% block breadcrumb %}<i class="pi pi-angle-right"></i> Assuntos{% endblock %}

{% block content %}
<div class="p-card">
    <div class="p-card-header">
        <h2 class="p-card-title"><i class="pi pi-tag"></i> Assuntos</h2>
        <button class="p-btn p-btn-success p-btn-sm btn-modal" data-url="{{ path('app_assunto_new') }}" data-title="Novo Assunto">
            <i class="pi pi-plus"></i> Novo Assunto
        </button>
    </div>
    <div class="p-card-body">
        <div class="dt-wrapper">
            <table class="p-datatable w-full" data-sortable="[0,1,2]">
                <thead>
                    <tr><th>#</th><th>Descrição</th><th>Criado em</th><th>Ações</th></tr>
                </thead>
                <tbody>
                    {% for assunto in assuntos %}
                    <tr>
                        <td>{{ assunto.id }}</td>
                        <td><strong>{{ assunto.descricao }}</strong></td>
                        <td>{{ assunto.createdAt|date('d/m/Y H:i') }}</td>
                        <td>
                            <div class="flex gap-2">
                                <button class="p-btn p-btn-warning p-btn-sm btn-modal" data-url="{{ path('app_assunto_edit', {id: assunto.id}) }}" data-title="Editar Assunto">
                                    <i class="pi pi-pencil"></i>
                                </button>
                                <form method="post" action="{{ path('app_assunto_delete', {id: assunto.id}) }}" class="d-inline">
                                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ assunto.id) }}">
                                    <button type="button" class="p-btn p-btn-danger p-btn-sm btn-delete" data-label='"{{ assunto.descricao }}"'>
                                        <i class="pi pi-trash"></i>
                                    </button>
                                </form>
                            </div>
                        </td>
                    </tr>
                    {% else %}
                    <tr><td colspan="4" class="text-center" style="color:var(--text-secondary);padding:2rem;">Nenhum assunto cadastrado.</td></tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>

{% include 'admin/_modals.html.twig' %}
{% endblock %}

{% block javascripts %}
<script src="/js/datatable.js"></script>
<script src="/js/modal-crud.js"></script>
{% endblock %}
