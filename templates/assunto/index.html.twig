{% extends 'admin/layout.html.twig' %}

{% block title %}Assuntos — {{ livraria.nome ?? 'Bookstore' }}{% endblock %}

{% block content %}
<div id="alert-container"></div>
<div class="d-flex justify-content-between align-items-center mb-4">
    <h2>Assuntos</h2>
    <button class="btn btn-success btn-modal" data-url="{{ path('app_assunto_new') }}" data-title="Novo Assunto">
        <i class="bi bi-plus-lg"></i> Novo Assunto
    </button>
</div>

<div class="card border-0 shadow-sm">
    <div class="table-responsive">
        <table class="table table-hover mb-0">
            <thead class="table-light">
                <tr>
                    <th>#</th>
                    <th>Descrição</th>
                    <th>Criado em</th>
                    <th class="text-end">Ações</th>
                </tr>
            </thead>
            <tbody>
                {% for assunto in assuntos %}
                <tr>
                    <td>{{ assunto.id }}</td>
                    <td>{{ assunto.descricao }}</td>
                    <td>{{ assunto.createdAt|date('d/m/Y H:i') }}</td>
                    <td class="text-end">
                        <button class="btn btn-sm btn-outline-warning btn-modal" data-url="{{ path('app_assunto_edit', {id: assunto.id}) }}" data-title="Editar Assunto">
                            <i class="bi bi-pencil"></i>
                        </button>
                        <form method="post" action="{{ path('app_assunto_delete', {id: assunto.id}) }}" class="d-inline" onsubmit="return confirm('Tem certeza que deseja excluir?')">
                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ assunto.id) }}">
                            <button class="btn btn-sm btn-outline-danger"><i class="bi bi-trash"></i></button>
                        </form>
                    </td>
                </tr>
                {% else %}
                <tr><td colspan="4" class="text-center text-muted py-4">Nenhum assunto cadastrado.</td></tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</div>

<!-- Modal -->
<div class="modal fade" id="crudModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title"></h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body"></div>
        </div>
    </div>
</div>
{% endblock %}

{% block javascripts %}
{% include 'admin/_modal_js.html.twig' %}
{% endblock %}
