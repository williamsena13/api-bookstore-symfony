{% extends 'admin/layout.html.twig' %}

{% block title %}Autores — {{ livraria.nome ?? 'Bookstore' }}{% endblock %}
{% block page_title %}Autores{% endblock %}
{% block breadcrumb %}<i class="pi pi-angle-right"></i> Autores{% endblock %}

{% block content %}
<div class="p-card">
    <div class="p-card-header">
        <h2 class="p-card-title"><i class="pi pi-user"></i> Autores</h2>
        <button class="p-btn p-btn-success p-btn-sm btn-modal" data-url="{{ path('app_autor_new') }}" data-title="Novo Autor">
            <i class="pi pi-plus"></i> Novo Autor
        </button>
    </div>
    <div class="p-card-body">
        <div class="dt-wrapper">
            <table class="p-datatable w-full" data-sortable="[0,1,2]">
                <thead>
                    <tr><th>#</th><th>Nome</th><th>Criado em</th><th>Ações</th></tr>
                </thead>
                <tbody>
                    {% for autor in autores %}
                    <tr>
                        <td>{{ autor.id }}</td>
                        <td><strong>{{ autor.nome }}</strong></td>
                        <td>{{ autor.createdAt|date('d/m/Y H:i') }}</td>
                        <td>
                            <div class="flex gap-2">
                                <button class="p-btn p-btn-warning p-btn-sm btn-modal" data-url="{{ path('app_autor_edit', {id: autor.id}) }}" data-title="Editar Autor">
                                    <i class="pi pi-pencil"></i>
                                </button>
                                <form method="post" action="{{ path('app_autor_delete', {id: autor.id}) }}" class="d-inline">
                                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ autor.id) }}">
                                    <button type="button" class="p-btn p-btn-danger p-btn-sm btn-delete" data-label='"{{ autor.nome }}"'>
                                        <i class="pi pi-trash"></i>
                                    </button>
                                </form>
                            </div>
                        </td>
                    </tr>
                    {% else %}
                    <tr><td colspan="4" class="text-center" style="color:var(--text-secondary);padding:2rem;">Nenhum autor cadastrado.</td></tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>

{% include 'admin/_modals.html.twig' %}
{% endblock %}

{% block javascripts %}
<script src="/js/datatable.js"></script>
<script src="/js/modal-crud.js"></script>
{% endblock %}
