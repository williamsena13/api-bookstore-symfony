{% extends 'admin/layout.html.twig' %}
{% block title %}Autores — {{ livraria.nome ?? 'Bookstore' }}{% endblock %}
{% block page_title %}Autores{% endblock %}
{% block breadcrumb %}<i class="pi pi-angle-right"></i> Autores{% endblock %}

{% block content %}
<div class="p-card">
    <div class="p-card-header">
        <h2 class="p-card-title"><i class="pi pi-user"></i> Autores</h2>
        <div class="d-flex align-items-center gap-2">
            <span style="font-size:.82rem;color:var(--text-secondary);">{{ paginacao.total }} autores</span>
            <button class="p-btn p-btn-success p-btn-sm btn-modal" data-url="{{ path('app_autor_new') }}" data-title="Novo Autor">
                <i class="pi pi-plus"></i> Novo Autor
            </button>
        </div>
    </div>

    <div class="p-card-body" style="padding:0;">
        <div class="dt-wrapper">
            <table class="table table-striped table-bordered table-hover mb-0">
                <thead>
                    <tr>
                        <th style="width:60px;">#</th>
                        <th>Nome</th>
                        <th style="width:110px;">Livros</th>
                        <th style="width:140px;">Criado em</th>
                        <th style="width:90px;">Ações</th>
                    </tr>
                </thead>
                <tbody>
                    {% for autor in paginacao.items %}
                    <tr>
                        <td class="text-muted">{{ autor.id }}</td>
                        <td><strong>{{ autor.nome }}</strong></td>
                        <td>
                            {% if autor.total_livros > 0 %}
                                <span class="p-badge p-badge-primary"><i class="pi pi-book"></i> {{ autor.total_livros }}</span>
                            {% else %}
                                <span class="p-badge p-badge-secondary">0</span>
                            {% endif %}
                        </td>
                        <td class="text-muted" style="font-size:.8rem;">{{ autor.created_at|date('d/m/Y H:i') }}</td>
                        <td>
                            <div class="flex gap-1">
                                <a href="{{ path('app_autor_edit', {id: autor.id}) }}" class="p-btn p-btn-warning p-btn-sm" title="Editar">
                                    <i class="pi pi-pencil"></i>
                                </a>
                                <form method="post" action="{{ path('app_autor_delete', {id: autor.id}) }}" class="d-inline">
                                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ autor.id) }}">
                                    <button type="button" class="p-btn p-btn-danger p-btn-sm btn-delete" data-label='"{{ autor.nome }}"' title="Excluir">
                                        <i class="pi pi-trash"></i>
                                    </button>
                                </form>
                            </div>
                        </td>
                    </tr>
                    {% else %}
                    <tr><td colspan="5" style="text-align:center;padding:2rem;color:var(--text-secondary);">Nenhum autor cadastrado.</td></tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>

    {% include 'admin/_pagination.html.twig' %}
</div>

{% include 'admin/_modals.html.twig' %}
{% endblock %}

{% block javascripts %}
<script src="/js/modal-crud.js"></script>
{% endblock %}
